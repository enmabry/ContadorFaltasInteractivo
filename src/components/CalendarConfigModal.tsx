import React from 'react';

/**
 * Clean stub to satisfy any pending Vite HMR cache requests
 * and prevent 404 console errors during active dev sessions.
 */
export const CalendarConfigModal: React.FC<Record<string, unknown>> = () => null;
export default CalendarConfigModal;
